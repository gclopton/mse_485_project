# -----------------------------
# OS / editor cruft
# -----------------------------
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.nfs*

# -----------------------------
# Python / notebooks
# -----------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.conda/
.ipynb_checkpoints/
.pytest_cache/

# -----------------------------
# Build / binaries
# -----------------------------
bin/
!bin/README.md
*.o
*.so
*.a

# -----------------------------
# Logs (project-wide)
# -----------------------------
logs/
scripts/logs/
**/*.log
**/*.out
**/*.err

# HPC scheduler logs (Slurm/PBS/LSF)
slurm-*.out
slurm-*.err
*.o[0-9]*
*.e[0-9]*

# -----------------------------
# LAMMPS runtime outputs
# (ignore runs/* by default; allow only small provenance + figures)
# -----------------------------
runs/*                # ignore everything under runs/ by default
!runs/**/             # but allow directory traversal

# keep only tiny provenance
!runs/**/manifest.yaml

# keep figures (small PNGs, PDFs)
!runs/**/figures/
!runs/**/figures/**

# still ignore all heavy artifacts anywhere under runs/
runs/**/dumps/
runs/**/post/
runs/**/thermo.dat
runs/**/ttm_core.dat
runs/**/*.dump
runs/**/*.lammpstrj
runs/**/logs/           # DO NOT track per-run logs
runs/**/inputs_snapshot/ # DO NOT track snapshots (duplicates repo inputs)

# -----------------------------
# Generated analysis artifacts
# (ignore by default but keep the Phase-1 minimal drop)
# -----------------------------
analysis/**/*.csv
analysis/**/*.npz
analysis/**/*.png
analysis/**/*.pdf
analysis/**/*.txt
!analysis/definitions.md
!analysis/templates/**
!analysis/README.md

# keep the small Phase-1 minimal deliverables
!analysis/phase1_minimal/
!analysis/phase1_minimal/**

# Top-level figures: keep curated ones
!figures/
!figures/**
# if you later mirror large sets here, you can re-ignore subfolders, e.g.:
# figures/phase2/raw/

# -----------------------------
# Inputs — keep sources, ignore generated structures by default
# -----------------------------
!inputs/ttm/**
!inputs/structure/POSCAR_CeO2_unit.vasp
inputs/structure/*.lmp
inputs/structure/*.json

# -----------------------------
# Environments
# -----------------------------
!env/environment.yml
env/*lock*
env/*.log

# -----------------------------
# Third-party bundles
# -----------------------------
.thirdparty/
